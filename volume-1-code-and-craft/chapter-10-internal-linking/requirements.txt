networkx>=3.2
requests>=2.32.0
lxml>=5.2.0
# networkx 3.x implements pagerank on top of scipy; without it the
# --pagerank flag fails at runtime (the error is caught and reported
# as pagerank_error, so the feature silently does nothing).
scipy>=1.11